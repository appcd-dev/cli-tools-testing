# Local Environment Configuration

# CLI Paths (common local development paths)
STACKGEN_CLI_PATH=/opt/homebrew/bin/stackgen
CLOUD2CODE_CLI_PATH=/opt/homebrew/bin/cloud2code

# Expected CLI Versions (latest/local versions)
STACKGEN_EXPECTED_VERSION=0.62.0
CLOUD2CODE_EXPECTED_VERSION=0.2.0

# AWS Configuration (local development regions)
AWS_PRIMARY_REGION=us-west-2
AWS_SECONDARY_REGION=us-east-1

# Appstack Configuration (local test appstack)
TEST_APPSTACK_ID=local-test-12345-67890-abcdef

# Test Data Paths (relative paths for local development)
TEST_DATA_PATH=./src/inputs
POLICY_BASE_PATH=
TFSTATE_BASE_PATH=

# Test Configuration (optimized for local development)
SKIP_INTEGRATION_TESTS=true
TEST_TIMEOUT=120000
OUTPUT_DIR=./local-outputs

# Environment Metadata
ENVIRONMENT_NAME=local
ENVIRONMENT_DESCRIPTION=Local development environment configuration 